<h3>Incomplete Invoices</h3>
<div class="incomplete_invoices">
  <% @invoices.incomplete_invoices.each do |invoice| %>
    <li><%= link_to "Invoice #{invoice.id}", admin_invoices_path(invoice) %> - Created: <%= invoice.created_at.strftime("%A, %B %d,%Y") %><li>
  <% end %>
</div>
